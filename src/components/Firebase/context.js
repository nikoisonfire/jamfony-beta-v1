import React from 'react';

/**
 *  Firebase Context and unnessary HOC Consumer (because you use hooks everywhere).
 **/

const FirebaseContext = React.createContext(null);

export default FirebaseContext;